# Required API Keys
COMPANIES_HOUSE_API_KEY=your_companies_house_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
RESEND_API_KEY=your_resend_api_key

# Database (required)
DATABASE_URL=postgresql://user:password@localhost:5432/distress_signal

# Gmail OAuth (required)
# Get credentials from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a project and enable Gmail API
# 2. Create OAuth 2.0 credentials (Desktop application)
# 3. Download JSON and paste the entire contents here (single line, escaped)
GMAIL_CREDENTIALS_JSON={"installed":{"client_id":"...","client_secret":"..."}}

# Gmail token is auto-populated after first OAuth flow - leave empty initially
GMAIL_TOKEN_JSON=

# Recipient email for enriched reports (required)
CLIENT_EMAIL=recipient@client.com

# CCOD download credentials (optional - for monthly property data sync)
CCOD_GOV_UK_CREDENTIALS=
